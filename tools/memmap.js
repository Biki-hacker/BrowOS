'use strict';

const RAM_BASE = 0x00000000;
const RAM_SIZE = 0x10000000;
const FRAME_SIZE = 0x00001000;
const NFRAMES = RAM_SIZE / FRAME_SIZE;
const KERNEL_LINK = 0x00000000;
const KERNEL_FRAMES = 0x00000200;
const KERNEL_MAX = 0x00100000;

const HEAP_START = 0x00100000;
const HEAP_END = 0x00200000;

const CLINT_BASE = 0x02000000;

const UART_BASE = 0x10000000;
const TIMER_BASE = 0x10001000;
const INTC_BASE = 0x10002000;
const BLOCK_BASE = 0x10003000;
const GPU_CTRL_BASE = 0x10004000;
const GPU_CMD_BASE = 0x10005000;
const MMIO_END = 0xFFFFF000;

const GPU_REG_MAGIC = 0x00;
const GPU_REG_VERSION = 0x04;
const GPU_REG_STATUS = 0x08;
const GPU_REG_FB_WIDTH = 0x0C;
const GPU_REG_FB_HEIGHT = 0x10;
const GPU_REG_FB_ADDR = 0x14;
const GPU_REG_CMD_ADDR = 0x18;
const GPU_REG_CMD_LEN = 0x1C;
const GPU_REG_SUBMIT = 0x20;
const GPU_REG_PRESENT = 0x24;
const GPU_REG_BACKEND = 0x28;

const GPU_MAGIC = 0x42475055; // 'BGPU'
const GPU_VERSION = 0x00010000;

const CMD_CLEAR = 1;
const CMD_DRAW_RECT = 2;
const CMD_BLIT = 3;
const CMD_DISPATCH_COMPUTE = 4;
const CMD_PRESENT = 5;

module.exports = {
  RAM_BASE,
  RAM_SIZE,
  FRAME_SIZE,
  NFRAMES,
  KERNEL_LINK,
  KERNEL_FRAMES,
  KERNEL_MAX,
  HEAP_START,
  HEAP_END,
  CLINT_BASE,
  UART_BASE,
  TIMER_BASE,
  INTC_BASE,
  BLOCK_BASE,
  GPU_CTRL_BASE,
  GPU_CMD_BASE,
  MMIO_END,
  GPU_REG_MAGIC,
  GPU_REG_VERSION,
  GPU_REG_STATUS,
  GPU_REG_FB_WIDTH,
  GPU_REG_FB_HEIGHT,
  GPU_REG_FB_ADDR,
  GPU_REG_CMD_ADDR,
  GPU_REG_CMD_LEN,
  GPU_REG_SUBMIT,
  GPU_REG_PRESENT,
  GPU_REG_BACKEND,
  GPU_MAGIC,
  GPU_VERSION,
  CMD_CLEAR,
  CMD_DRAW_RECT,
  CMD_BLIT,
  CMD_DISPATCH_COMPUTE,
  CMD_PRESENT,
};