{
  "name": "browos",
  "version": "0.1.0",
  "description": "BrowOS — a complete tiny virtual computer implemented inside one HTML file",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "build": "node tools/build.js",
    "smoke": "node tools/smoke.js",
    "check": "node --test \"tests/*.test.js\" && node tools/build.js && node tools/smoke.js"
  },
  "engines": {
    "node": ">=18"
  }
}
