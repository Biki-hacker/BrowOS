{
  "name": "browos",
  "version": "0.1.0",
  "description": "BrowOS — a complete tiny virtual computer implemented inside one HTML file",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "build": "node tools/bundle.js",
    "check": "node --test \"tests/*.test.js\" && node tools/bundle.js"
  },
  "engines": {
    "node": ">=18"
  }
}
